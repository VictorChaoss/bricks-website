import urllib.request, json, os

api_key = os.environ.get('OPENROUTER_API_KEY', '') # wait I don't have the key locally!
# I don't have their API key, so I can't test image generation locally unless they provide it.
